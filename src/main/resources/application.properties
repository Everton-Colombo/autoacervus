spring.application.name=autoacervus

# Selenium properties:
webdriver.chrome.driver=webdrivers/chromedriver

# Datasource properties:
#spring.jpa.hibernate.ddl-auto=update   # Atualiza a schema, se necessário. Usar com cdd
spring.datasource.url=jdbc:mysql://localhost:3306/autosbu
spring.datasource.username=dev
spring.datasource.password=Dev-896651
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# Força o hibernate a utilizar os nomes especificados nas annotations (era pra ser padrão, mas tava dando problema)
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# Logging properties
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true